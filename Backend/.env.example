# MongoDB Atlas connection string
MONGODB_URI="mongodb+srv://araghospital:7sl9UmhrzIBm28qV@cluster0.example.mongodb.net/araghospital?retryWrites=true&w=majority"

# JWT secrets (generate strong random strings - see README)
JWT_ACCESS_SECRET="REPLACE_WITH_STRONG_RANDOM_HEX"
JWT_REFRESH_SECRET="REPLACE_WITH_STRONG_RANDOM_HEX"

# Tokens expiry
JWT_ACCESS_EXPIRES="15m"
JWT_REFRESH_EXPIRES="30d"

# SMTP (optional)
SMTP_HOST=smtp.mailprovider.com
SMTP_PORT=587
SMTP_USER=your-smtp-user
SMTP_PASS=your-smtp-pass
FROM_EMAIL="no-reply@araghospital.com"

# S3 (optional) - if not set, uploads use local ./uploads/
S3_ENDPOINT=
S3_BUCKET=
S3_ACCESS_KEY=
S3_SECRET_KEY=

APP_URL=http://localhost:4000
PORT=4000
